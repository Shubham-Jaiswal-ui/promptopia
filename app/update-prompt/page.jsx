import UpdatePrompt from "@components/UpdatePrompt";
import React, { Suspense } from "react";

const Page = () => {
	return (
		<Suspense>
			<UpdatePrompt />
		</Suspense>
	);
};

export default Page;
